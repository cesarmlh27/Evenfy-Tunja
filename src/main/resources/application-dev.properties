# ========== DATABASE (DEV) ==========
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/evenfy_db}
spring.datasource.username=${DB_USERNAME:user_api}
spring.datasource.password=${DB_PASSWORD:1234}

# ========== EMAIL (DEV FALLBACKS) ==========
spring.mail.username=${MAIL_USERNAME:dev@example.com}
spring.mail.password=${MAIL_PASSWORD:dev-password}

# ========== JWT (DEV FALLBACK) ==========
jwt.secret=${JWT_SECRET:dev-only-change-this-secret-key-32chars!!}

# ========== RESEND (DEV FALLBACK) ==========
resend.api.key=${RESEND_API_KEY:dev-resend-key}

# ========== CORS (DEV) ==========
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,http://localhost:8080}
